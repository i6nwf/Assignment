public class Third {
//2.7If we choose an increment of 128, how many calls to the nextValue method from the ArithmeticProgression class
// of Section 2.2.3 can we make before we cause a long-integer overflow?
//A long-integer overflow occurs when the value of a long variable exceeds the maximum representable value,
// which is 2^63 .
//n ≈ (2^63 - 1) / 128 ≈ 7.18 x 10^12 calls.
}
